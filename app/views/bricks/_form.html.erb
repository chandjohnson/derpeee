<%= form_for(@brick) do |f| %>
  <% if @brick.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@brick.errors.count, "error") %> prohibited this brick from being saved:</h2>

      <ul>
      <% @brick.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.text_area :string, :maxlength => 15 %>
  </div>
  <p>
    <div class="actions">
      <%= f.submit %>
    </div>
  </p>
<% end %>
